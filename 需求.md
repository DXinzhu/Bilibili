## 示例
1.点击XXX(例如点击QQ音乐首页的"我喜欢")->维护点击"我喜欢"的历史记录
2.数一下我有多少个待收货订单->维护一个函数,函数计算订单数量
CheckOrderNumber(AI_answer)
     step1. 计算真实的待收货订单数量,例如fact_num
     step2. Check if AI_answer == fact_num
3.计算10.20武汉往返长沙的的高铁票总价->维护一个函数,函数计算真实价格
CheckTicketPrice(AI_answer)
     step1. 计算真实的总价,例如fact_num
     step2. Check if AI_answer == fact_num

## 当前指令
在我的页面,点击历史记录入口,查看观看历史。

## 当前指令的检验逻辑
查看观看历史->维护一个函数,验证观看历史记录是否可访问
CheckWatchHistory()
     step1. 从设备读取观看历史数据文件(watch_history.json)
     step2. 验证历史记录文件是否存在且包含数据
     step3. 检查历史记录列表是否非空(至少包含一条观看记录)
     step4. 验证历史记录数据结构完整性(每条记录包含videoId, videoTitle, watchTime等必要字段)

## 你当前的任务
参考示例指令对应的检验逻辑,对当前指令生成检验逻辑,填写在需求.md中
AutoTest文件夹下有eval_1.py,对应的指令是给北大李老师发送Hello,检验逻辑是判断我和北大李老师的聊天记录里面有没有这个"hello",请修改python代码为当前指令的自动化检验的脚本。
